apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "frontendchart.fullname" . }}-test-connection"
  labels:
    {{- include "frontendchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "frontendchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
